{
	"name": "nftyswaps",
	"license": "UNLICENSED",
	"scripts": {
		"codegen": "graph codegen",
		"build": "graph build",
		"deploy": "graph deploy --debug --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ mauricechristopher/Nftyswaps",
		"create-local": "graph create --node http://localhost:8020/ mauricechristopher/Nftyswaps",
		"remove-local": "graph remove --node http://localhost:8020/ mauricechristopher/Nftyswaps",
		"deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mauricechristopher/Nftyswaps"
	},
	"dependencies": {
		"@graphprotocol/graph-cli": "0.20.0",
		"@graphprotocol/graph-ts": "0.20.0"
	}
}
